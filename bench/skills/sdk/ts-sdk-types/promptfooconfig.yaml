# Eval config for ts-sdk-types
providers:
  - id: anthropic:messages:claude-sonnet-4-5-20250514
    config:
      temperature: 0
      max_tokens: 8192

prompts:
  - file://prompt.json

tests: file://tests.yaml
